# Changelog — Model 1D v0.3.2.post2

## Fixed

- Declared `tabulate>=0.9.0` as a MacroPulse runtime dependency.
- Prevents `DataFrame.to_markdown()` from failing during temporal report
  generation.

No model, tournament, temporal-policy, or database logic changed.
