# Changelog — Model 1D v0.2.0.post1

## Fixed

- Replaced the invalid `forecast_registry.production_model_name` reference in
  the historical integration-test fixture with the actual schema column
  `champion_model`.

No model or production database logic changed.
